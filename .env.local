# In development, omit MONGODB_URI to use the embedded MongoDB server with local data under `.cache/mongodb`.
# For a real local server, start `mongod` and uncomment the line below:
# MONGODB_URI=mongodb://127.0.0.1:27017/industrial-form-builder
SESSION_MAX_AGE_DAYS=7
